@model IEnumerable<CarpoolingCR.Models.Locations.Province>

@{
    ViewBag.Title = "Index";
}

<h3>Index</h3>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CountriyId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CountriyId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ProvinceId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ProvinceId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ProvinceId })
        </td>
    </tr>
}

</table>
