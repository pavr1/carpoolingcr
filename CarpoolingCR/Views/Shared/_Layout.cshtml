<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BuscoRide.com</title>
    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")

    <link rel="stylesheet" href="~/fonts/jquery-ui-1.12.1.custom/jquery-ui.min.css">

    <script src="~/fonts/jquery-ui-1.12.1.custom/external/jquery/jquery.js"></script>
    <script src="~/fonts/jquery-ui-1.12.1.custom/jquery-ui.min.js"></script>

    <link rel="stylesheet" type="text/css" href="~/fonts/datetimepicker-master/jquery.datetimepicker.css"/ >

    <link href="~/fonts/jquery-ui-1.12.1.custom/jquery-ui.css" rel="stylesheet" />
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.7.2/css/all.css" integrity="sha384-fnmOCqbTlWIlj8LyTjo7mOUStjsKC4pOpQbqyi7RrhN7udi9RwhKkMHpvLbHG9Sr" crossorigin="anonymous">

    <script src="~/fonts/datetimepicker-master/build/jquery.datetimepicker.full.min.js"></script>
    <link rel="stylesheet" href="https://rawgithub.com/indrimuska/jquery-editable-select/master/dist/jquery-editable-select.min.css">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/9.6.0/highlight.min.js"></script>
    <script src="https://rawgithub.com/indrimuska/jquery-editable-select/master/dist/jquery-editable-select.min.js"></script>
    <script src="~/Scripts/List.js"></script>
    @*<link rel="stylesheet" href="~/Content/processingmodal.css">*@

    <style type="text/css">
        input[type="text"] {
            border-radius: 25px
        }

        input[type="submit"] {
            border-radius: 25px
        }

        input[type="password"] {
            border-radius: 25px
        }

        input[type="button"] {
            border-radius: 25px
        }

        a.btn {
            border-radius: 25px !important;
        }

        select {
            border-radius: 25px !important;
        }
    </style>


    <style>
        /* Style The Dropdown Button */
        .dropbtn {
            background-color: black;
            color: white;
            padding: 16px;
            font-size: 16px;
            border: none;
            cursor: pointer;
        }

        /* The container <div> - needed to position the dropdown content */
        .dropdown {
            position: relative;
            display: inline-block;
        }

        /* Dropdown Content (Hidden by Default) */
        .dropdown-content {
            display: none;
            position: absolute;
            background-color: #f9f9f9;
            min-width: 160px;
            box-shadow: 0px 8px 16px 0px rgba(0,0,0,0.2);
            z-index: 1;
        }

            /* Links inside the dropdown */
            .dropdown-content a {
                color: black;
                padding: 12px 16px;
                text-decoration: none;
                display: block;
            }

                /* Change color of dropdown links on hover */
                .dropdown-content a:hover {
                    background-color: #f1f1f1
                }

        /* Show the dropdown menu on hover */
        .dropdown:hover .dropdown-content {
            display: block;
        }

        /* Change the background color of the dropdown button when the dropdown content is shown */
        .dropdown:hover .dropbtn {
            background-color: black;
        }
    </style>
</head>
<body>
    @{if (User != null && User.Identity != null && User.Identity.IsAuthenticated)
        {
            <div class="navbar navbar-inverse navbar-fixed-top">
                <div class="container">
                    <div class="navbar-header">
                        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                            <span class="icon-bar"></span>
                            <span class="icon-bar"></span>
                            <span class="icon-bar"></span>
                        </button>
                        @Html.ActionLink("www.buscoridecr.com", "Index", "Home", new { area = "" }, new { @class = "navbar-brand" })
                    </div>
                    <div class="navbar-collapse collapse">
                        @{
                            if (User.Identity.IsAuthenticated)
                            {
                                <ul class="nav navbar-nav">
                                    <li>@Html.ActionLink("Menú Principal", "ValidateUserRol", "UserRols")</li>
                                </ul>
                            }
                        }
                        @Html.Partial("_LoginPartial")
                    </div>
                    <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12" style="border-radius:50px;">
                        <p class="" style="border-radius:50px; color:orangered" id="errorMsgContainer"></p>
                        <p class="" style="border-radius:50px; color:yellow" id="warningMsgContainer"></p>
                        <p class="" style="border-radius:50px; color:lightgreen" id="infoMsgContainer"></p>
                    </div>
                </div>
            </div>
        }
        else
        {
            <div class="navbar navbar-inverse navbar-fixed-top" style="border-radius: 0px 0px 15px 15px">
                <div class="container">
                    <div class="navbar-header">
                        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                            <span class="icon-bar"></span>
                            <span class="icon-bar"></span>
                            <span class="icon-bar"></span>
                        </button>
                        @Html.ActionLink("www.buscoridecr.com", "Index", "Home", new { area = "" }, new { @class = "navbar-brand" })
                    </div>
                    <div class="navbar-collapse collapse">
                        @Html.Partial("_LoginPartial")
                    </div>
                </div>
            </div>
        }
    }
    <div class="container body-content">
        @RenderBody()
        <hr />
        <footer>
            <p><img src="~/Content/Icons/Car.png" style="width:50px; height:15px" /> &copy; @DateTime.Now.Year - www.buscoride.com</p>
        </footer>
    </div>
    <div class="modal fade" id="loadMe" tabindex="-1" role="dialog" aria-labelledby="loadMeLabel">
        <div class="modal-dialog modal-sm" role="document">
            <div class="modal-content">
                <div class="modal-body text-center">
                    <div class="loader"></div>
                    <div clas="loader-txt">
                        <p>Check out this slick bootstrap spinner modal. <br><br><small>We are addicted to Bootstrap... #love</small></p>
                    </div>
                </div>
            </div>
        </div>
    </div>

    @Scripts.Render("~/bundles/bootstrap")
    @RenderSection("scripts", required: false)

    <script>
        $(document).ready(function () {
            checkMessages();
        });

        function checkMessages() {
            var error = '@ViewBag.Error';
            var warning = '@ViewBag.Warning';
            var info = '@ViewBag.Info';

            if (error != '') {
                setTimeout(function () {
                    $('#errorMsgContainer').slideUp();
                    $('#errorMsgContainer').html('&nbsp;&nbsp;&nbsp;' + error);
                    $('#errorMsgContainer').slideDown();

                    ScrollUp();

                    setTimeout(function () {
                        $('#errorMsgContainer').slideUp();
                    }, 10000);
                }, 100);
            }

            if (warning != '') {
                setTimeout(function () {
                    $('#warningMsgContainer').slideUp();
                    $('#warningMsgContainer').html('&nbsp;&nbsp;&nbsp;' + warning);
                    $('#warningMsgContainer').slideDown();

                    ScrollUp();

                    setTimeout(function () {
                        $('#warningMsgContainer').slideUp();

                    }, 10000);
                }, 100);
            }

            if (info != '') {
                setTimeout(function () {
                    $('#infoMsgContainer').slideUp();
                    $('#infoMsgContainer').html('&nbsp;&nbsp;&nbsp;' + info);
                    $('#infoMsgContainer').slideDown();

                    ScrollUp();

                    setTimeout(function () {
                        $('#infoMsgContainer').slideUp();

                    }, 10000);
                }, 100);
            }
        }

        function displayMessages(message, type) {
            if (type == 'error') {
                setTimeout(function () {
                    $('#errorMsgContainer').slideUp();
                    $('#errorMsgContainer').html('&nbsp;&nbsp;&nbsp;' + message);
                    $('#errorMsgContainer').slideDown();

                    ScrollUp();

                    setTimeout(function () {
                        $('#errorMsgContainer').slideUp();
                    }, 10000);
                }, 100);
            }

            if (type == 'warning') {
                setTimeout(function () {
                    $('#warningMsgContainer').slideUp();
                    $('#warningMsgContainer').html('&nbsp;&nbsp;&nbsp;' + message);
                    $('#warningMsgContainer').slideDown();

                    ScrollUp();

                    setTimeout(function () {
                        $('#warningMsgContainer').slideUp();

                    }, 10000);
                }, 100);
            }

            if (type == 'info') {
                setTimeout(function () {
                    $('#infoMsgContainer').slideUp();
                    $('#infoMsgContainer').html('&nbsp;&nbsp;&nbsp;' + message);
                    $('#infoMsgContainer').slideDown();

                    ScrollUp();

                    setTimeout(function () {
                        $('#infoMsgContainer').slideUp();

                    }, 10000);
                }, 100);
            }
        }

        function ScrollUp() {
            $('html, body').animate({
                scrollTop: 0
            }, 1000);
        }
    </script>
</body>
</html>
