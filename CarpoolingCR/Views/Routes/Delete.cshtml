@model CarpoolingCR.Models.Route

@{
    ViewBag.Title = "Delete";
}

<h2>Rutas</h2>
<meta name="viewport" content="width=device-width, user-scalable=no">
<h4>Está seguro que desea eliminar "@Html.DisplayFor(model => model.Name)"?</h4>
<div>
    <hr />

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            @Html.ActionLink("<<", "Index", new { }, new { @class = "btn btn-primary" }) <input type="submit" value="Eliminar" class="btn btn-danger pull-right" />
        </div>
    }
</div>
